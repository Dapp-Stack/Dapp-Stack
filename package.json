{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "0.1.0",
  "devDependencies": {
    "lerna": "^3.1.1",
    "npm-run-all": "^4.1.3",
    "eslint": "^5.4.0",
    "lint-staged": "^7.2.2",
    "prettier": "^1.14.2"
  },
  "lint-staged": {
    "*.js": [
      "prettier --trailing-comma es5 --single-quote --write",
      "git add"
    ],
    "yarn.lock": [
      "git rm --cached"
    ]
  }
}
